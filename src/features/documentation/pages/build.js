import React from 'react'

export default function Build() {
    return (
        <div className='container-fluid bg-help'>
            <h2 className='title-help'>Saving Projects</h2>
            <p className='content-help'>The Build option compiles and creates TCP files depending on the specification of various compiler options in the Project Properties window, like Show warnings , Treat Warning as error , and so on.\nIndividual files can be compiled, but cannot be built as TCP. A project must be created for building a TCP file.
            </p>
        </div>
    )
}
